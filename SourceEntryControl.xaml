<UserControl x:Class="Forumcord.SourceEntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="64" d:DesignWidth="64" MouseRightButtonUp="UserControl_MouseRightButtonUp" MouseLeftButtonUp="UserControl_MouseLeftButtonUp">
    <Grid>
        <!-- Define the appearance of the Source entry -->
        <Image Width="64" Height="64" Source="/images/forumwhite.png"/>
        <Image Source="{Binding ForumImage}" Width="64" Height="64"/>
        <TextBlock Text="{Binding ForumName}" VerticalAlignment="Bottom" Width="64" Foreground="White" Height="16"/>
    </Grid>
</UserControl>
